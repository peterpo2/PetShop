# UI/UX Pro Max Notes - ZOO BAKALIA

## Command Used

```powershell
python .codex\skills\ui-ux-pro-max\scripts\search.py "local pet supplies shop yellow blue red friendly neighborhood static website" --design-system -p "ZOO BAKALIA"
```

## Useful Output

The generated direction emphasized:

- social proof and trust
- accessible and ethical UI
- rounded, friendly typography
- clear above-fold CTA
- strong focus states
- 44px touch targets
- reduced motion support
- responsive checks at 375px, 768px, 1024px, and 1440px

## Project Adaptation

The generated palette is not used directly because `PetShopLogo.png` already defines the brand. Use the generated guidance for structure and quality checks, while keeping:

- warm yellow as primary brand color
- saturated blue for headings, buttons, and graphic structure
- coral/red for CTA accents
- graphite for text
- cream/white for readability

## Production UI Checklist

- Bulgarian language copy
- logo visible in header and footer
- one primary CTA per major section
- phone CTA uses `tel:0888284284`
- Google Maps CTA opens the real address
- mobile sticky CTA for call or location
- no horizontal scroll
- visible keyboard focus
- semantic headings
- descriptive image alt text
- optimized logo and images with fixed dimensions/aspect ratios
- `prefers-reduced-motion` respected
